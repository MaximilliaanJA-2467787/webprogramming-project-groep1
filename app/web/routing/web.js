const BaseRouter = require('../../base/router');

class WebRouter extends BaseRouter {
    constructor(express) {
        super(express);
    }

    bind() {
        super.bind();
    }
}

module.exports = WebRouter;
